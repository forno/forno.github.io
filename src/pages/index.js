import React from "react";

import Layout from "../components/layout";

const IndexPage = () => (
  <Layout pageTitle="Home" createdYear="2017">
    <h2>FORNO's portfolio</h2>
    <p>Now avalable are Tools only.</p>
  </Layout>
);

export default IndexPage;
